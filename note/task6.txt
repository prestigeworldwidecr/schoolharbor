Task 6 — Small Helper Function (Optional, 3–5 min if time allows)
Write select_latest(df, key_cols, date_col) that returns the latest row per key (by date_col).
Demonstrate it on assessments to reproduce the Spring latest-per-key from Task 2.